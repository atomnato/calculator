 calculation -- принимает клик на кнопку расчет, рассчитывает переменные для скриптов и запускает скрипты.

 drow -- рассчитывает координаты для рисунка "одна четверть".
 dr -- рисует изображения "одна четверть"

 drowMonth и drMonth -- тоже самое но для рисунка "полусолнце"

 check_input -- проверяет корректность ввода данных пользователем и ограничивает их количество.

 wig -- отвечает за прокрутку на полученном изображении.
